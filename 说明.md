# Nintendont 使用说明

本文档说明 **金手指（作弊码）** 与 **各类控制器** 的用法，适用于本仓库（含 `zh` 汉化菜单分支）在 Wii / Wii U（vWii）上通过 USB Loader 等启动的 Nintendont。

---

## 一、金手指（作弊码）

### 1.1 支持的格式

Nintendont 游戏运行时 **仅加载 `.gct` 文件**。Loader 菜单可从 Gecko 标准 **`.txt` 码表** 生成 GCT（与 USB Loader GX 格式相同）。

| 类型 | 路径（与游戏同启动盘） |
|------|------------------------|
| **TXT 码表（编辑用）** | `{usb或sd}:/txtcode/{6位ID}.txt` |
| **GCT（游戏用）** | `{usb或sd}:/codes/{6位ID}.gct`（与 UGX/WiiFlow 一致） |

**Loader 内生成（游戏列表按 Y/2）：**

1. 将码表放到 `txtcode/GM8E01.txt` 等  
2. 游戏列表选中游戏 → **Y / 2** 进入金手指菜单  
3. **A** 开关各项，**X** 生成 GCT 到 `codes/`  
4. 成功后自动打开 **「作弊码」**（使用内核默认 `/codes/` 查找，**无需**「作弊码路径」）

GCT 文件头为两次 `00D0C0DE`，之后为 8 字节一组的地址与数据，由内置 **codehandler** 在游戏运行时按 Ocarina 规则写入内存。

### 1.2 菜单中的设置

进入 **设置**（游戏列表界面按 **B** 切换）后，左栏相关项：

| 菜单项 | 配置标志 | 说明 |
|--------|----------|------|
| **作弊码** | `NIN_CFG_CHEATS` | 打开后，内核会在启动游戏时尝试加载 GCT 并挂接 Hook |
| **作弊码路径** | `NIN_CFG_CHEAT_PATH` | 高级：使用 `nincfg.bin` 里自定义 **`CheatPath`**（一般用默认 `/codes/` 即可） |

操作方式：光标移到对应行，按 **A** 在「打开 / 关闭」之间切换。

**推荐：** 用 **Y/2 金手指菜单** 从 `txtcode/` 生成 `codes/*.gct` 后，只开 **「作弊码」** 即可，不必开「作弊码路径」。

**注意：**

- 在游戏列表中 **上下换游戏** 时，若同时开着「作弊码」和「作弊码路径」，会自动 **关闭这两项**（自定义路径防串游戏）。用 `/codes/ID.gct` 时通常只开「作弊码」。

### 1.3 GCT 文件应放在哪里

未使用自定义路径时，内核按 **当前游戏路径** 与 **6 位 Game ID** 依次查找（找到即用）：

1. `{游戏所在目录}/game.gct`  
   例：`usb:/games/GM8E01.gcm` → `usb:/games/game.gct`
2. `{游戏所在目录}/{6位ID}.gct`  
   例：`usb:/games/GM8E01.gct`
3. `/codes/{6位ID}.gct`（设备根目录，经典 Gecko 布局）  
   例：`usb:/codes/GM8E01.gct`
4. `/games/{6位ID}/{6位ID}.gct`（旧版备份布局）  
   例：`usb:/games/GM8E01/GM8E01.gct`

**多碟游戏** 目录示例：

```
usb:/games/我的游戏/
  game.iso      ← 碟 1
  disc2.iso     ← 碟 2
  game.gct      ← 或 GM8E01.gct 等
```

自定义路径时：在 `nincfg.bin` 中填写例如 `usb:/codes/GM8E01.gct`，并开启「作弊码路径」。

### 1.4 生效条件与限制

- 必须开启 **「作弊码」**。
- 以下情况 **不会** 加载 GCT：
  - 部分 Triforce 街机模式（`TRI_SB`）
  - 通过 IPL 启动的路径（`useipl`）
- GCT 体积不能超过内核为 codehandler 预留的内存区，过大时日志会提示 “Cheatfile is too large”。
- **Wii U** 上无 USB Gecko **调试器** 功能，但金手指仍可用（使用 `codehandleronly`）。

### 1.5 推荐流程

1. 确认游戏 **6 位 ID**（游戏信息界面或文件名，如 `GM8E01`）。
2. 将对应 `.gct` 放到上表任一路径（推荐 `codes/游戏ID.gct` 或与 iso 同目录的 `game.gct`）。
3. 设置里打开 **「作弊码」**。
4. 选择游戏并按 **A** 启动。
5. 若无效：检查 GCT 是否为本游戏、ID 是否匹配、是否误开了「作弊码路径」但路径为空。

配置保存在设备根目录及启动设备上的 **`nincfg.bin`**（与 `apps/Nintendont/` 内配置同步写入）。

---

## 二、控制器总览

| 类型 | 平台 | 说明 |
|------|------|------|
| Wii 遥控器 / 经典手柄 / Wii U 经典手柄 | Wii、Wii U | 蓝牙，菜单与游戏均可映射为 GC 手柄 |
| **Wii U GamePad** | 仅 Wii U（vWii） | 通过 Wii DRC 库映射，可指定模拟几号手柄 |
| **USB HID 手柄** | Wii、Wii U | Xbox/PS/罗技等，需 `controller.ini` 或内置表 |
| **任天堂 GC 转接器** | Wii、Wii U | USB，VID/PID `057e:0337`，有专用支持 |
| **原生 GC 手柄口** | 仅 Wii | 机身四个口；Wii U 无此接口 |
| **GBA 连接线（Native Control）** | 仅 Wii | 与 HID/蓝牙互斥 |

---

## 三、GameCube 按键对照（读组合键用）

Nintendont 内部统一模拟 **GameCube 手柄** 的 `PADStatus`。下表为源码中的位掩码（`global.h`），组合键均按这些位判断：

| GC 名称 | 掩码 | 常见实体键 |
|---------|------|------------|
| 十字左/右/下/上 | `0x0001`～`0x0008` | 方向键 |
| **Z**（肩键，非 Wii ZL） | `0x0010` | GC Z / Nunchuk **Z** |
| **R**（右肩，数字键） | `0x0020` | GC R |
| **L**（左肩，数字键） | `0x0040` | GC L |
| **A** | `0x0100` | A |
| **B** | `0x0200` | B |
| **X** | `0x0400` | X |
| **Y** | `0x0800` | Y |
| **Start** | `0x1000` | Start / 遥控器 **1** / 经典 Start |

另有模拟量：**L/R 扳机**（`triggerLeft` / `triggerRight`）、主摇杆、C 摇杆。

---

## 四、Loader 菜单映射（`FPad.c`）

菜单 **不直接读 GC 口**，而是由 **FPad** 汇总：Wii 遥控器 / 经典 / Wii U Pro（WUPC）/ Wii U GamePad（DRC）/ USB HID（经 `PADReadGC`）/ Wii 机身 GC 口（仅 Wii）。

### 4.1 菜单通用表

| 菜单功能 | 对应 GC 概念 | Wii 遥控器（横持） | 经典 / 经典 Pro / Wii U Pro | Wii U GamePad | USB / 原生 GC |
|----------|--------------|-------------------|----------------------------|---------------|---------------|
| 确认 | A | **A** | **A** | **A** | **A** |
| 返回 / 进设置 | B | **B** | **B** | **B** | **B** |
| 游戏信息 | X | **1** | **X** | **X** | **X** |
| 上/下/左/右 | 方向 | 方向键；经典/Pro 还可 **左摇杆** | 同左 | 方向键 / 左摇杆 | 方向键 / 摇杆 |
| 返回设备选择 | Start/Home | **Home** | **Home** | **Home** | **Start** |
| 在线更新（设置内） | X | **1** | **X** | **X** | **X** |
| 截图（需编译开启 `SCREENSHOT`） | — | 同时 **+** 与 **-** | — | 同时 **+** 与 **-** | — |
| 关机请求 | — | Wii **电源键**；GamePad **关机请求** | 同左 | 同左 | ini 可配 **Power** |

### 4.2 各界面操作

**游戏列表：**

| 按键 | 作用 |
|------|------|
| **A** | 启动当前游戏 |
| **B** | 进入 **设置** |
| **X / 1** | **游戏信息** |
| **Home / Start** | 返回设备选择 |
| 方向键 / 摇杆 | 移动光标 |

**设置：**

| 按键 | 作用 |
|------|------|
| **A** | 切换开关或数值 |
| **B** | 返回游戏列表 |
| 方向键 | 移动光标；**左右** 在左栏 / 右栏间切换 |
| **X** | 在线更新（若可用） |

**说明：** 纯 Wii 遥控器 **无 Nunchuk/经典** 时，**菜单可用**（FPad 直接读 WPAD）；进入游戏后需扩展控制器或 USB/GC 口（见下文）。

---

## 五、游戏中映射（`PADReadGC.c` + `kernel/BT.c`）

游戏运行时由 **`PADRead` 钩子** 把各类输入写成 GC `PADStatus`。以下按手柄类型说明。

### 5.1 原生 GameCube 手柄（仅 Wii 机身四口）

| 项目 | 说明 |
|------|------|
| 映射方式 | **1:1**，直接读 SI 总线，无重映射 |
| 摇杆/扳机 | 带死区处理；支持 **大金刚鼓（Bongo）** 特殊滤波 |
| 玩家位 | 由 **「最大手柄数」**（0～4）与物理插口决定 |
| 组合键 | 与其它手柄相同（见 **第八节**） |

### 5.2 USB HID 手柄 / GC USB 适配器

**配置文件：** 游戏盘根目录 `controllers/VID_PID.ini` 或 `/controller.ini`（详见 `controllerconfigs/`）。

**ini 字段 → GC：**

| ini 键 | 映射到 GC |
|--------|-----------|
| `A` `B` `X` `Y` | 面键 |
| `L` `R` | 左/右肩 **数字键** + 对应模拟扳机 |
| `ZL` | 与 `L` 同按时为 **半按 L**（`0x7F`），否则全按 |
| `Z` | GC **Z** 键（`0x0010`） |
| `S` | **Start** |
| `Left/Right/Up/Down` 或 `DPAD=1` 帽形方向 | 十字键 |
| `StickX/Y` | 主摇杆 |
| `CStickX/Y` | C 摇杆 |
| `LAnalog` `RAnalog` | 模拟 L/R 扳机（`DigitalLR=1` 时为数字扳机） |
| `Power` | 全部按下时可 **退出到 stub**（`DoExit`） |

**任天堂 GC 适配器（`057E:0337`）：**

- 四端口原生协议，按键布局与 **原装 GC 手柄一致**。
- 支持 WaveBird、大金刚鼓检测、DDR 跳舞毯方向优化等特殊逻辑。

### 5.3 Wii 经典控制器 / 经典 Pro / Wii U Pro（蓝牙）

识别为 `C_CC`（经典）或 `C_CCP`（经典 Pro，数字扳机）。

| 实体键 | GC |
|--------|-----|
| A / B / X / Y | A / B / X / Y |
| 方向键 | 十字键 |
| L / R / ZL / ZR | L / R / Z（ZR→Z）/ 模拟扳机 |
| Start / Home | Start；**Home → 退出游戏** |
| 左摇杆 / 右摇杆 | 主摇杆 / C 摇杆 |

**运行时：**

- 经典手柄 **Select（`-`）**：在 **ABXY 正常** 与 **顺时针旋转一档**（B→A, Y→B, A→X, X→Y）之间切换（`C_SWAP`）。
- 设置 **「Wiimote控制器 震动」**：震动落在 **Wii 遥控器本体**（非经典手柄马达）。

### 5.4 Wii 遥控器横持（仅菜单；游戏内需扩展）

| 实体键 | Loader 菜单（FPad） | 游戏内（蓝牙 `BTPad`） |
|--------|---------------------|-------------------------|
| A / B | 确认 / 返回 | **无扩展时不作为 GC 手柄**（`used == C_NOT_SET`） |
| 1 / 2 | X / （FPad 有 Y，菜单少用） | — |
| 方向键 | 移动光标 | — |
| Home | 返回上一层 | — |
| + / - | 截图组合 | — |

**结论：** 想用手柄玩游戏，请接 **Nunchuk**、**经典手柄**，或使用 **USB / GC 口**。

### 5.5 Wii 遥控器 + Nunchuk（7 种方案）

连接 Nunchuk 后内核启用 **IR + 加速度**（`0x37` 报告）。默认方案为 **Howard 通用（方案 0）**。

**切换方法（游戏前或连接后）：按住遥控器 `2`，再按下列组合；松开 `2` 后生效。**

| 按住 `2` + | 方案 | 名称（源码注释） | 要点 |
|------------|------|------------------|------|
| （仅 `2`，再松开） | 0 | Howard 通用 | A→A, B→B, Z→Z, +→X, -→Y；倾斜→L/R 扳机；按住 **C**→倾斜控 C 摇杆 |
| `2` + **←** | 1 | AbdallahTerro | C→X, Z→Y, -→Z, +→R |
| `2` + **→** | 2 | naggers | 未按 C：↑→Z, ↓→B, →→X, ←→Y；Z/C→L/R |
| `2` + **↑** | 3 | 赛车（前后倾斜） | 与 0 类似，但 **前后倾** 控油门/刹车 |
| `2` + **↓** | 4 | 赛车（A 作油门） | A→Z（油门）；倾斜逻辑同 0 |
| `2` + **-** | 5 | Troopage | 未按 C：+→X, -→B, Z→L, B→R，**方向键→C 摇杆** |
| `2` + **1** | 6 | FPS（IR 控 C 摇杆） | IR 指哪 C 摇杆哪；方向键映射因 **C** 而异 |

**额外实时切换（仍按住 `2` 时）：**

| 组合 | 作用 |
|------|------|
| `2` + **C**（边沿） | 经典式 **ABXY 旋转**（`C_SWAP`） |
| `2` + **Z**（边沿） | 开关 **IR 指针控 C 摇杆**（`C_ISWAP`） |
| 遥控器 **1** | **Start** |
| 遥控器 **Home** | **退出游戏** |

Nunchuk 摇杆：主摇杆；IR 两点时右摇杆区域由光条坐标模拟（FPS 方案）。

### 5.6 Wii U GamePad（`wiidrc`，仅 vWii）

在设置 **「WiiU Gamepad Slot」** 指定玩家 **1～4**（`None` 表示不用 GamePad 当 GC 手柄）。

| 实体键 | GC |
|--------|-----|
| A / B / X / Y | A / B / X / Y |
| 方向键 | 十字键 |
| L / R | 肩键；与 **ZL/ZR** 同按时为 **半按** 对应扳机 |
| ZL / ZR | 左/右 **模拟扳机**（全按） |
| **R**（右肩面键） | GC **Z** |
| **+** | **Start** |
| **Home** | **退出游戏** |
| 左/右摇杆 | 主摇杆 / C 摇杆 |

**运行时：**

- 点按 **-（Minus）**：在 **正常 ABXY** 与 **顺时针旋转 ABXY** 间切换（与经典 Select 类似）。
- **关机键**：请求关机 → `DoShutdown`（见第八节）。

GamePad 占用槽位时，USB HID 会自动 **避开** 该端口，以免抢 1P。

---

## 六、快捷键与系统操作总表

以下以 **GameCube 语义** 为准（Wii 遥控器需先映射到 GC 位，见第五节）。

### 6.1 游戏内（`PADReadGC.c`）

| 组合 | 掩码 | 作用 |
|------|------|------|
| **Start + Z + R** | `0x1030` | **返回 Loader**（`RESET_STATUS = 0x3DEA`） |
| **B + R + 十字下** | `0x234` | **退出到 stub**（`DoExit`，`0x1DEA`） |
| **Home**（经典 / Wii U Pro / 遥控器 Home） | — | **退出到 stub**（`DoExit`） |
| Wii U GamePad **Home** | — | **退出到 stub** |
| GamePad / Wii **关机请求** | — | **关机**（`DoShutdown`，`0x7DEA`） |
| USB ini **Power** 全按 | — | **退出到 stub**（若 ini 配置了 `Power`） |

**注意：** README 常写作「R+Z+B+下」，源码实际检测的是 **`B + R + 下`**（`0x234`），其中 **R** 为 GC **右肩数字键**（`0x0020`），不是 Nunchuk 的 Z。Nunchuk 的 Z 在 GC 侧为 **Z 扳机键**（`0x0010`）。

### 6.2 Loader 菜单（`FPad.c` / `menu.c`）

| 操作 | 作用 |
|------|------|
| **Home / Start** | 返回设备选择 |
| Wii **电源键** | 关机流程 |
| GamePad **关机请求** | 关机流程 |
| **+** 与 **-** 同时 | 截图 → `Screenshot.png`（需编译 `SCREENSHOT`） |

### 6.3 组合键在各手柄上的按法速查

| 目的 | 原生 GC / USB 适配器 | 经典 / Pro | Nunchuk（方案 0） | GamePad |
|------|---------------------|------------|-------------------|---------|
| 返回 Loader | Start+Z+R | 同左 | **1**+Nunchuk **Z**+右肩 **R** | **+** + 右肩 **R** + **ZR** |
| 退出 stub | B+R+下 | 同左 | **B**+右肩 **R**+下 | **B**+**ZR**+下 |
| 菜单退出层 | — | Home | Home | Home |

（Nunchuk 的 Z → GC **Z**；遥控器 **B** → GC **B**。）

---

## 七、原生 GameCube 与其它 Wii 专用功能

### 7.1 机身四口与 USB 适配器

- **Wii**：四口直连 + USB `057e:0337` 适配器（每适配器最多 4 手柄）。
- **Wii U**：无机身 GC 口，请用 USB 适配器或 GamePad / 蓝牙。

### 7.2 Wii 独有（与手柄相关）

| 功能 | 说明 |
|------|------|
| **GBA控制** | 开启后占用 SI，**蓝牙 + USB HID 不可用** |
| **真实记忆卡** | 关闭记忆卡模拟后使用卡槽 |
| **麦克风 / Bongo** | 部分游戏专用设备 |

---

## 八、设置项与控制器相关速查

| 设置（汉化菜单） | 与控制器的关系 |
|------------------|----------------|
| 最大手柄数 | 仅 **Wii**：原生 GC 口数量上限 |
| GBA控制 | 仅 **Wii**：开启后禁用蓝牙 + USB HID |
| Wiimote控制器 震动 | 经典手柄操作时 Wii 遥控器震动 |
| WiiU Gamepad Slot | 仅 **Wii U**：GamePad 对应玩家编号 |
| 日志 | 排查 HID ini 是否加载 |

---

## 九、常见问题

**Q：金手指不生效？**  
检查 `.gct` 路径、游戏 ID、是否开启「作弊码」、换游戏后是否被自动关闭、文件是否过大。

**Q：USB 手柄按键乱？**  
添加正确的 `controllers/VID_PID.ini`，或从 `controllerconfigs` 复制模板修改。

**Q：Wii U 上能用 Wii 遥控器吗？**  
可以配对；**菜单**可用横持遥控器。**游戏**需接 Nunchuk/经典手柄，或使用 USB 适配器 / GamePad。

**Q：返回 Loader / 关机的组合键按了没反应？**  
确认按的是 **Start+Z+R**（返回 Loader）与 **B+R+十字下**（退出 stub），不是 README 里偶尔写错的「B+Z+下」。Nunchuk 的 Z 对应 GC 的 **Z 扳机**（`0x0010`），不是右肩 **R**（`0x0020`）。

**Q：经典手柄没有震动？**  
尝试打开 **「Wiimote控制器 震动」**（震动在 Wii 遥控器上）。

**Q：能同时用 GamePad + USB 手柄吗？**  
可以；在 **WiiU Gamepad Slot** 中把 GamePad 设为 2P/3P 等，避免与 USB 手柄抢 1P。

---

## 十、参考路径（源码）

| 功能 | 主要文件 |
|------|----------|
| 配置结构 | `common/include/CommonConfig.h` |
| 作弊查找 / 加载 | `kernel/Patch.c` → `Check_Cheats()` |
| GCT 执行 | `codehandler/codehandleronly.s` |
| 菜单输入 | `loader/source/FPad.c` |
| TXT→GCT（Loader） | `loader/source/gct.c`、`loader/source/CheatMenu.c` |
| 游戏内映射 / 组合键 | `loader/source/ppc/PADReadGC/source/PADReadGC.c` |
| 蓝牙连接 / Nunchuk 方案切换 | `kernel/BT.c` |
| USB 手柄 | `kernel/HID.c`、`controllerconfigs/*.ini` |
| 持久配置 | `loader/source/global.c`、`nincfg.bin` |

---

*文档版本与 `zh` 分支汉化菜单一致；若合并上游英文菜单，设置项名称可能仍为英文，逻辑相同。*
